package:
  name: shadow_work
  version: "0.0.0"

source:
  path: ../..

build:
  number: 0

requirements:
  build:
    - python
    - setuptools

  run:
    - python

test:
  requires:
    - pytest
  imports:
    - shadow_work

about:
  home: add_url_here
  license: MIT License
